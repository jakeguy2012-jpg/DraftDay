import { NextResponse } from "next/server";import { z } from "zod";import { recordManualPick } from "@/server/draft-sessions";const schema=z.object({playerId:z.string(),teamId:z.string().optional()});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;return NextResponse.json({data:await recordManualPick(id,schema.parse(await request.json()))},{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Pick failed"},{status:409})}}
