import {NextResponse} from "next/server";import {syncSleeperDraft} from "@/server/sleeper-live";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;return NextResponse.json({data:await syncSleeperDraft(id)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Sleeper synchronization failed"},{status:502})}}
