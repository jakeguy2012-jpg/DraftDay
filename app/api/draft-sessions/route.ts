import { NextResponse } from "next/server";import { z } from "zod";import { createManualSession } from "@/server/draft-sessions";const schema=z.object({leagueId:z.string(),teams:z.number().int().min(2).max(32),rounds:z.number().int().min(1).max(40),userSlot:z.number().int().min(1),type:z.enum(["SNAKE","LINEAR"])});export async function POST(request:Request){try{return NextResponse.json({data:await createManualSession(schema.parse(await request.json()))},{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to start draft"},{status:400})}}
