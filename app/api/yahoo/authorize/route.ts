import { NextResponse } from "next/server";import { randomBytes } from "node:crypto";import { yahooAuthorizationUrl } from "@/providers/yahoo";export async function GET(){try{const state=randomBytes(24).toString("hex");const response=NextResponse.redirect(yahooAuthorizationUrl(state));response.cookies.set("yahoo_oauth_state",state,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:600});return response}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Yahoo OAuth unavailable"},{status:503})}}
