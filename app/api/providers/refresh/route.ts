import {NextResponse} from "next/server";import {z} from "zod";import {refresh} from "@/server/refresh";const schema=z.object({target:z.enum(["players","league","adp","projections","vegas","all"])});export async function POST(request:Request){try{return NextResponse.json({data:await refresh(schema.parse(await request.json()).target),refreshedAt:new Date().toISOString()})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Provider refresh failed"},{status:502})}}
