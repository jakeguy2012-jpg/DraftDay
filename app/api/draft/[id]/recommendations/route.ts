import {NextResponse} from "next/server";import {demoPlayers} from "@/lib/demo";import {recommendation} from "@/services/draftiq";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;return NextResponse.json({data:demoPlayers.map(p=>({...p,...recommendation(p,47)})).sort((a,b)=>b.draftIq-a.draftIq),meta:{draftId:id,demo:true,updatedAt:new Date().toISOString()}})}
