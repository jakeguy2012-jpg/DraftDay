import { NextResponse } from "next/server";import { leagueProfile } from "@/server/leagues";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,data=await leagueProfile(id);return data?NextResponse.json({data}):NextResponse.json({error:"League not found"},{status:404})}catch{return NextResponse.json({error:"Database unavailable"},{status:503})}}
